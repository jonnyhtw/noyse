
fail_fast: true

repos:
  - repo: local
    hooks:
 
      - id: jupytext
        name: "Verify: Python files compile (syntax check"
        language: python
        entry: python
        args: [-m, compileall, -q, .]
        pass_filenames: false

